services:
  web:
    image: agceramoz/agcera-ms-bn:latest
    # Loads all variables from .env into the container
    env_file:
      - .env
    # Interpolates variables for use in the compose configuration
    ports:
      - '${PORT}:${PORT}'
