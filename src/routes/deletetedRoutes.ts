// import deletedController from '@src/controllers/deletedController';
// import { isStoreKeeperUp } from '@src/middlewares/checkAuth';

// import { Router } from 'express';

// const router = Router();

// router.get('/', isStoreKeeperUp, deletedController.getAllDeleted);
// // router.get('/:id', isStoreKeeperUp, deletedController.getOneTransaction);

// export default router;
